package domains

// Domains used throughout kited and services
const (
	PrimaryHost = "kite.com"

	Alpha        = "alpha." + PrimaryHost
	DocsAPI      = "docsapi." + PrimaryHost
	GaStaging    = "ga-staging." + PrimaryHost
	RemoteConfig = "rc." + PrimaryHost
	Staging      = "staging." + PrimaryHost
	Telemetry    = "t." + PrimaryHost
	WWW          = "www." + PrimaryHost
)
