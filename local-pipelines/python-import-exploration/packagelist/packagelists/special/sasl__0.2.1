FROM kiteco/python-import-exploration

RUN apt update && apt install --no-install-recommends -y libsasl2-dev
RUN rm -rf /var/lib/apt/lists/*

RUN pip2 install sasl==0.2.1

ENTRYPOINT ["/usr/bin/env", "python2", "-m", "kite.pkgexploration", "sasl", "0.2.1", "sasl__0.2.1.json", "sasl__0.2.1.log"]
